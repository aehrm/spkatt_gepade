Peft model folders should be placed here:
- `cue_model_peft`
- `cue_joiner_peft`
- `role_model_peft`

Either train the peft models yourself, or download the published models. (See main README for instructions.)
